---

---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>Document</title>
  </head>
  <body>
    <a href="../tests">Index</a>
    <div class="layout-container">
      <h1>Gradient Fill</h1>
      <h2>Text Shadow</h2>
      <div class="card"><h3>Backlit Card</h3></div>
      <div class="border-card"><h5>Border Gradient</h5></div>
    </div>
    <style>
      body {
        width: clamp(10vw, 20em, 100vw);
        height: 100vh;
        margin: min(10vh, 10%);
        /* padding: min(8vw; 8%); */
        background: #ffffff;
        /* display: flex;
            flex-direction: column;
            gap: 1rem;
            justify-content: center;
            align-items: center; */
      }

      .layout-container {
        width: min(1000px, 100%);
        margin: 0 auto;
        columns: 3 300px;
        column-gap: 1em;
      }
      h1 {
        font-size: clamp(1.8rem, calc(4vw + 1rem), 5rem);
        background: linear-gradient(160deg, #2623dc, #d144e1);
        background-clip: text;
        -webkit-background-clip: text;
        color: transparent;
      }
      h2 {
        font-size: 2.5rem;
        color: white;
        /* horizontal|vertical shadow|blur|color */
        text-shadow: 2px 2px 10px red;
      }
      h3 {
        font-size: 2rem;
        text-shadow:
          -1px -1px 0 #000,
          1px -1px 0 #000,
          -1px 1px 0 #000,
          1px 1px 0 #000;
        color: white;
      }
      .card {
        width: 300px;
        height: 200px;
        background: #fffff0;
        border-radius: 10px;
        padding: 0.2rem;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
      }
      .border-card {
        width: 300px;
        height: 30px;
        padding: 0.2rem;
        border-radius: 10px;
        background: #fffff0;
        display: flex;
        justify-content: center;
        align-items: center;
        border: 8px solid transparent;
        border-image: linear-gradient(60deg, #2623dc, #d144e1) 1;
      }
      .card:hover {
        /* box-shadow: none|h-offset v-offset blur spread color |inset|initial|inherit; */
        box-shadow:
          8px 8px 20px 2px rgba(0, 0, 0, 0.1),
          -8px -8px 10px white;
      }
    </style>
  </body>
</html>
